ifeq ($(CONFIG_COMPONENTS_AW_TINY_ALSA_LIB),y)
include $(AW_SRCDIR)$(DELIM)drivers$(DELIM)rtos-hal$(DELIM)hal$(DELIM)source$(DELIM)sound$(DELIM)component$(DELIM)aw-tiny-alsa-lib$(DELIM)Make.defs
endif
